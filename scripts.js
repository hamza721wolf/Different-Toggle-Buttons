function toggleEffect(effectType) {
    const button = document.querySelector('.bounce-btn');
    button.classList.toggle(effectType);
}

function toggleEffect(effectType){
    const button = document.querySelector('.fade-btn');
    button.classList.toggle(effectType);
}

